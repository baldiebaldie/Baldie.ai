{
  "name": "baldie-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/web\" \"npm run dev -w apps/brain\"",
    "build": "npm run build -w apps/web && npm run build -w apps/brain"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
